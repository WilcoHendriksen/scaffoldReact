import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
    return <div style={{width: '100vw', height: '100vh'}}>
        scafoldded react app
    </div>
}

ReactDOM.render(
    <App /> ,
    document.getElementById("root")
)